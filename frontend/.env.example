# Base URL of the Spring Boot backend. Leave blank in local dev - Vite proxies
# /api to http://localhost:8080 (see vite.config.js).
VITE_API_BASE_URL=

# Must match app.api-key / APP_API_KEY on the backend.
VITE_APP_API_KEY=local-dev-key
